{
  "name": "circuit-trader",
  "version": "0.1.0",
  "private": true,
  "description": "Circuit Trader — an autonomous BNB-chain trading agent that cannot trade unless its own signed risk constitution allows it. BNB HACK: AI Trading Agent Edition.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm -w packages/policy run test && npm -w packages/connectors run test && npm run test:runner",
    "test:runner": "npm run build:policy && node --test scripts/durable-execution.test.mjs",
    "build:policy": "npm -w packages/policy run build",
    "build:connectors": "npm run build:policy && npm -w packages/connectors run build",
    "dev": "npm -w apps/web run dev",
    "build:web": "npm run build:connectors && npm -w apps/web run build",
    "constitution:sign": "npm run build:policy && node scripts/sign-constitution.mjs",
    "live:check": "npm run build:connectors && node scripts/live-runner.mjs --check",
    "live:once": "npm run build:connectors && node scripts/live-runner.mjs --once",
    "live:runner": "npm run build:connectors && node scripts/live-runner.mjs",
    "live:reconcile": "npm run build:policy && node scripts/reconcile-execution.mjs"
  }
}
